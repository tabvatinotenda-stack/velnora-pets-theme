<div class="sec">
  <h1 class="sec-title">{{ page.title }}</h1>
  <div class="sec-rule"></div>
  <div class="page-content">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "Page Content"
}
{% endschema %}
